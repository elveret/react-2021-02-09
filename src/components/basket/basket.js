import React from 'react';
import { connect } from 'react-redux';
import styles from '../menu/menu.module.css';
import Product from '../product';

const Basket = ({ sumOrder }) => {
  return <div></div>;
};

export default connect(mapStateToProps)(Basket);
